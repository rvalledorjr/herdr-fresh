---
name: Bug report
about: Something in herdr-fresh isn't working as documented
title: "[bug] "
labels: bug
---

**herdr version:** (`herdr --version`)
**fresh version:** (`fresh --version`)
**OS:** (Linux / macOS / Windows — and distro/version if relevant)
**herdr-fresh version:** (from `herdr plugin list --json`, or the commit/tag you're on)

**What happened**

**What you expected**

**Steps to reproduce**
1.
2.
3.

**Relevant output**
Paste any error output, `herdr plugin log list` output, or `fresh --cmd daemon list` output that
seems relevant. Redact anything sensitive (paths, tokens).

**Anything else**
e.g. custom `config.toml` contents (redact `fresh_bin` if it's a private path), non-default
keybindings, whether this is a fresh install or an upgrade.
